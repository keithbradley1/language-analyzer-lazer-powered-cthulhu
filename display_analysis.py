from sentiment_identifier import *
from domain_identifier import *
from behavior_predictor import *

sentiment_identifier  =  positive_counter + negative_counter + neutral_counter

print ("The message" + {} + "has sentiment values of" + display sentiment values here + ", domain values of " + display domain values here + ", and behavior results of " + display behavior results here + ".")